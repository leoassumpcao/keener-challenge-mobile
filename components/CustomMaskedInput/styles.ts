import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#FFFFFF",
    width: "100%",
    borderColor: "#eaeaea",
    borderWidth: 1,
    borderRadius: 5,
    marginVertical: 5,
    padding: 15,
  },
});
